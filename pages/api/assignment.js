import prisma from "../../prisma/prisma";

async function parseCounties(req, res) {
    // this will take the legislators passed in and parse their counties. 
    return
}

async function getIdealVolunteers(req, res) {
    // this will get the ideal volunteers and newspapers for the given counties
    // it will also assign the highest-rated newspaper to volunteers that are available in the given categories
}

export default handler;